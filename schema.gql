type Query {
  greeting: String
  students: [Student]
  studentById(id: ID!): Student
}

type Student {
  id: ID!
  fullName: String
  major: Major
}

enum Major {
  BIOLOGY
  ENGINEERING
  SCIENCE
}
